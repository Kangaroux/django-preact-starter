import { h } from "preact";

import { Routes } from "../routes";


/**
 * The root component of the app.
 */
export const App = () => {
    return (
        <div>
            <Routes />
        </div>
    );
}
